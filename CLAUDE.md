# plugs

Pure Data(.pd)パッチ集。

## バグ調査時の心構え — 結論を出す前に検証する

- 原因を結論として提示する前に、関係する全ファイルを実際に最後まで読み切ること。プロジェクト自身のファイルだけでなく、挙動の根拠にする場合はライブラリ/vendor側のabstractionも確認し、一般的な知識だけで断定しないこと
- 提示する原因が、実際に報告された症状と一致するか確認してから結論とすること。症状に一致しない仮説は検証不足のサインとして扱う
- 手早く外れる結論を何度も出すより、じっくり1回で正確に調べることを優先する。静的な読解だけでは確信が持てない場合は、その旨と具体的な次の検証手段を明示し、推測を断定的な結論として提示しない
- ファイルの存在・内容・履歴について言及する際は、書く直前に実際に確認すること(`ls`、`git log`、ファイルの読み込みなど)。過去のセッションでの確認内容や記憶を、再確認せずに事実として書かないこと

## 変更を提示する前のセルフレビュー — ユーザーに粗探しさせない

- .pdパッチを機械生成・編集した後、「動く」ことの検証(connect範囲・inlet充足・構文エラー無し等)だけで終わらせず、提示前に**冗長さ・重複**が無いかを自分で洗い出すこと。特に:
  - 同じオブジェクトの同じ出力から、実質同じ値を別名の`s`(send)で二重に送っていないか(例: `s FooValue`と`s FooToParam`が同じknobの同じ出力から同じタイミングで送られている、など)
  - 既存のsend/receive名を再利用できる箇所で、新しい名前を安易に追加していないか
  - 同じ処理・同じ配線パターンを複数箇所に手で書いていて、1箇所にまとめられないか
- こうした重複は、機能的には正しく動いてしまうため動作確認だけでは見つからない。「これは本当に必要か、既存の何かで代用できないか」を都度自問してから完了報告すること
- ユーザーから同種の指摘を受けた後は、次回以降そのチェックを自発的に行い、同じ種類の指摘を再度させないこと

## 見慣れないPd/plugdataオブジェクトを使う前に — 記憶や類推で書かない

- 過去に使ったことのないオブジェクト、あるいは細部(生成引数の並び・空欄の書き方・接続本数)に確信が持てないオブジェクトは、**書く前に**該当オブジェクトの公式ヘルプファイル(`~/Documents/plugdata/Versions/*/Documentation/`配下、または`5.reference`)を実際に開いて確認すること。「たぶんこうだろう」で書いてから動作確認で直す、という順序は精度を落とす
- 特に注意が必要な誤りやすいポイント(実際にこのプロジェクトで踏んだ例):
  - **空欄記法はライブラリごとに違う**: ELSE系オブジェクト(`knob`、`bng`等)は空欄フィールドに`empty`を使うが、vanilla Pdのatom box(`floatatom`/`symbolatom`/`listbox`)は`-`を使う。`floatatom`/`symbolatom`のreceive/send欄に誤って`empty`(同じ文字列)を両方入れると、Pdは「同名のsend/receiveによる無限ループ」とみなして接続そのものをブロックする。片方のライブラリの慣習をもう片方に転用しない
  - `prepend`はvanillaには無く`cyclone/prepend`としてのみ存在する場合がある(バージョン依存)。`list prepend`の方が安全な代替
  - `pack`の生成引数は「型指定子」(`f`/`s`/`p`または数値)であり、任意の文字列で定数シンボル値を初期化することはできない
  - `makefilename`は1オブジェクトにつき`%`置換を1箇所しか処理できない(複数の値を組み込みたい場合はオブジェクトをカスケードするか、メッセージボックスの`$1`/`$2`置換を使う)
- 複数の新しいオブジェクトを組み合わせた配線チェーンを一度に組み立てて動かそうとしないこと。1つずつ検証してから次を足す方が、失敗した時にどこが原因か切り分けやすい。ただし「1つずつ」の対象は**新しく足した部分だけでなく、まだ動作確認していない箇所すべて**であること(次の項目参照)
- **症状が変わらないなら、変えた側(source)は原因ではない可能性が高い**: あるオブジェクトへの接続がエラーになり、接続元を作り直しても同じ文言のエラーが出続ける場合、原因は接続元ではなく**接続先(destination)の設定**にある可能性を疑うこと。同じ症状(同じエラー文言)が2〜3回続いたら、source側の再設計を続けるのをやめ、destination側の生成引数を1行ずつ読み直す。「動かないから作り直す」を繰り返す前に、「何が変わっていないか」を先に特定すること
- 「動かない」報告を受けたら、まず**plugdataのConsoleパネルの実際のエラー文言**を確認してもらうこと。Pd自身が原因を名指しすることが多く(例: `bad type`、`connection failed`、`argument number out of range`、`atom with same send/receive name`)、これを見ずに配線ロジックを何度も推測で書き直すのは非効率。エラー文言を確認できたら、それが指す2つのオブジェクト(src/dst双方)の生成引数を両方読み直すこと。片方だけを疑って読み飛ばさない
- ユーザーによる実機での動作確認は貴重なので、大きな再設計のたびに1回消費するのではなく、**変数を1つに絞った切り分けテスト**(例: 疑わしい表示先オブジェクトに、まずは繋ぎ方を変えずメッセージボックスから直接値を送ってみるだけ、等)に使うよう組み立てること

